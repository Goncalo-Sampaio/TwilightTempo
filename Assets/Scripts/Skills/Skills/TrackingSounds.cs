using UnityEngine;

public class TrackingSounds : MonoBehaviour, ISkill
{
    public void Cast()
    {
        Debug.Log("Tracking Sounds");
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
