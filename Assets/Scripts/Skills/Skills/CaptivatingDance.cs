using UnityEngine;

public class CaptivatingDance : MonoBehaviour, ISkill
{
    public void Cast()
    {
        Debug.Log("Captivating Dance");
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
